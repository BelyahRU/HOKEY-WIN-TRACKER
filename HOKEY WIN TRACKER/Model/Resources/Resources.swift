
import Foundation
import UIKit

enum Resources {
    enum Icons {
        enum UnselectedIcons {
            static let homeIcon = "homeIcon"
            static let matchesIcon = "matchesIcon"
            static let newsIcon = "newsIcon"
            static let statisticsIcon = "statisticsIcon"
            static let tableIcon = "tableIcon"
        }
    }
    
    enum Colors {
        static let blueColor = UIColor(red: 2/255, green: 60/255, blue: 86/255, alpha: 1.0)
    }
}
